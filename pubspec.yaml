name: texgrill_app
description: TexGrill - App Ufficiale con Sistema Fedeltà e Ordinazioni
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_localizations:
    sdk: flutter

  # FIREBASE - ✅ Versioni COMPATIBILI per Replit
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  firebase_auth: ^5.3.1
  cloud_functions: ^5.6.2  # ⬅️ AGGIORNATO da 4.0.7 a 5.6.2

  # UI & GRAFICA
  qr_flutter: ^4.1.0
  flutter_fortune_wheel: ^1.3.1
  cupertino_icons: ^1.0.8
  carousel_slider: ^5.0.0
  google_fonts: ^6.2.1
  
  # UTILITIES
  crypto: ^3.0.5
  intl: ^0.20.0  # ⬅️ Aggiornato da 0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # ⬅️ Aggiornato da 3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/onboarding1.png
    - assets/onboarding2.png
    - assets/onboarding3.png
